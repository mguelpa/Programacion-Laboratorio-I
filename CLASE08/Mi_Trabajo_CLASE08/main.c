#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

#define MAX_CHAR 50
#define MAX_PRODUCTOS 200

typedef struct
{
    char nombre[MAX_CHAR];
    char descripcion[MAX_CHAR];
    float precio;
    int isEmpty ;
}sProducto;

int main()
{
    sProducto item[MAX_PRODUCTOS];

    return 0;
}

int cargaProductos(sProducto* producto)
{
    for(i=0; i<s
    )
}
