CADENA DE CARACTERES

funciones utiles en biblioteca "string.h"

strlen(); le pasamos un array y nos devuelve la cantidad de caracteres que 		  contiene el array.

strcpy(); le pasamos un array de caracteres y lo copia en otro de un origen 	  a un destino

strncpy(a, b, n); copia el string de un orifen a un destino incluyendo el 		  tamaño del array las versiones con "n" se le puede 		  ingresar el limite lo que la hace mas segura de utilizar 		  por si el origen es"dudoso"
strcat();

strncat(); 

fgets(cadena, 16, stdin);

numero = atoy(cadena); me pasa los caracteres a numerico entonces puedo 			       sumar los numero que se encuntran escritos como 		       caracteres




ALGORITMOS DE ORDENAMIENTO:

metodo de insersion:

se pone un flag en 0 que cada vez que hay un cambio en el ordenamiento se pone en 1 y hace una nueva pasda la condicion de corte es cuando ya no hay elementos por ordenar la variable vuelve a 0 y corta


the big "o" mide la cantidad de swaps que tiene un algoritmo







TAREA TRATAR DE FABRICAR LAS FUNCIONES

 SCANF() , STRCPY(), STRLEN(). 